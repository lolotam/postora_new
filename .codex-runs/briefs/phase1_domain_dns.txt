<role>
You are a read-only verifier. You make HTTP/DNS/TLS observations and report them. You do NOT modify any file, do NOT deploy, do NOT restart anything, do NOT change DNS, do NOT edit env, do NOT commit. You only READ and OBSERVE.
</role>

<environment>
You are on Windows (git-bash). Available tools: curl (Windows curl.exe), nslookup. `dig` is NOT available — use nslookup. Do NOT rely on openssl being present; use `curl -vI` to capture TLS cert subject/issuer lines from stderr.
</environment>

<strict_rules>
- READ-ONLY. No file writes, no edits, no commits, no deploys, no container restarts, no DNS/env changes.
- Do NOT print secrets, tokens, API keys, auth header values, passwords, or the contents of any .env file.
- Only print: HTTP status codes, safe response headers (Server, Via, Date, Content-Type, x-served-by if present), TLS cert subject/issuer, and resolved DNS A records (IPs only).
- Keep output concise and tabular.
</strict_rules>

<task>
PHASE 1 — Domain and DNS check.

For EACH of these URLs, perform an HTTPS request and report:
  - final HTTP status code (after redirects)
  - redirect chain (if any)
  - TLS certificate: subject (CN) and issuer (O), and whether the cert validates OK (curl exit 0 / no cert error) yes/no
  - any safe identity header (Server, Via, x-kong, x-served-by, etc.) that hints at the service identity

URLs:
  1. https://api.postora.cloud
  2. https://supabase.postora.cloud
  3. https://postora.cloud
  4. https://old.postora.cloud

DNS verification (use nslookup):
  A. Resolve api.postora.cloud  -> list A record IPs
  B. Resolve efruibswazzuuupgyzmf.supabase.co -> list A record IPs
  C. Resolve supabase.postora.cloud -> list A record IPs
  D. Resolve postora.cloud -> list A record IPs
  E. Resolve old.postora.cloud -> list A record IPs

Then determine the OLD-HOST RISK:
  - Does api.postora.cloud resolve to the SAME IP (or same /24 netblock) as efruibswazzuuupgyzmf.supabase.co?
  - If the IPs match -> "api old-host risk: YES" (api.postora.cloud still points at old hosted Supabase).
  - If the IPs differ -> "api old-host risk: NO" (api.postora.cloud points elsewhere, e.g. self-hosted Kong).

Suggested command shapes (adapt as needed, keep them read-only):
  - Status + headers:     curl -sS -o /dev/null -D - -L --max-time 20 "https://<host>" | grep -iE '^(HTTP/|server:|via:|location:|x-kong|x-served-by|content-type:)'
  - Redirect chain:       curl -sS -o /dev/null -w "%{http_code} -> %{url_effective}\n" -L --max-time 20 "https://<host>"
  - Cert subject/issuer:  curl -vI --max-time 20 "https://<host>" 2>&1 | grep -iE 'subject:|issuer:|SSL certificate|verify'
  - DNS:                  nslookup <host>

</task>

<report_contract>
At the end, print EXACTLY this section (fill in values), then stop:

=== PHASE 1 REPORT ===
DOMAIN TABLE:
| host | http_status | cert_OK | cert_subject_CN | service_identity_hint |
| api.postora.cloud | <code> | yes/no | <CN> | <hint> |
| supabase.postora.cloud | ... | ... | ... | ... |
| postora.cloud | ... | ... | ... | ... |
| old.postora.cloud | ... | ... | ... | ... |

DNS:
- api.postora.cloud -> <ips>
- efruibswazzuuupgyzmf.supabase.co -> <ips>
- supabase.postora.cloud -> <ips>
- postora.cloud -> <ips>
- old.postora.cloud -> <ips>

API OLD-HOST RISK: YES / NO
- reason: <one line: do the IPs match the old hosted supabase or not>

CERT/ROUTING ISSUES:
- <bullet list of any cert mismatches, redirects, or routing oddities; or "none observed">

NOTES:
- <any caveats, e.g. timeouts, tools unavailable>
=== END PHASE 1 REPORT ===
</report_contract>
