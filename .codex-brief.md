<task>
Harden the Facebook OAuth and Instagram OAuth edge functions so they fail cleanly on unauthenticated, malformed, or method-incorrect requests instead of returning HTTP 500.

Current state:
- facebook-oauth (supabase/functions/facebook-oauth/index.ts): When called without auth, `await req.json()` on line 293 throws because there's no body. The catch on line 737 returns HTTP 500 "Authentication failed. Please try again." Body parsing on line 293 happens BEFORE method and auth checks.
- instagram-oauth (supabase/functions/instagram-oauth/index.ts): Same root cause on line 26 — `await req.json()` before any method or auth check. Returns HTTP 500 "Unexpected end of JSON input".
- Both functions already have OPTIONS preflight returning 200 at the top (correct).
- A shared cors.ts helper exists at supabase/functions/_shared/cors.ts with: corsHeaders, handleCorsOptions(), jsonResponse(), errorResponse(), unauthorizedResponse(), badRequestResponse().
- A shared auth-helper.ts exists at supabase/functions/_shared/auth-helper.ts with: authenticateCaller().
- See twitter-oauth/index.ts for a reference pattern: it parses body safely: `const body = await req.json().catch(() => ({}))` and checks method before body parsing.
- check-connection-health/index.test.ts shows the test pattern: Deno integration tests hitting the deployed function with assertEquals().

What to change:
1. supabase/functions/facebook-oauth/index.ts — restructure request handling to:
   - Keep OPTIONS preflight first
   - Check method: non-POST returns 405 (Method Not Allowed) before any body parsing
   - Parse body safely: `const body = await req.json().catch(() => ({}))` — never throw on body parsing
   - After method check and body parsing, check Authorization header: missing/malformed returns 401 early
   - All error responses use proper HTTP status codes (400, 401, 405, 500) with safe JSON error messages
   - No broad catch(500) that masks specific errors
   - Must not expose env var values (app secret, tokens) in responses or logs
   - Missing required env vars (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, FACEBOOK_APP_ID) return 500 with generic message
   - Non-POST requests: return 405 "Method not allowed" (not 400)
   - Authenticated valid request with proper body continues to work exactly as before (generate OAuth redirect, process callbacks, etc.)

2. supabase/functions/instagram-oauth/index.ts — same hardening:
   - Same safe body parsing pattern
   - Same method check (405 for non-POST)
   - Same auth check early (401 for missing/malformed)
   - Same safe error responses
   - Instagram-specific env vars: INSTAGRAM_APP_ID, INSTAGRAM_APP_SECRET

3. Create test files following the existing Deno integration test pattern from check-connection-health/index.test.ts:
   - supabase/functions/facebook-oauth/index.test.ts
   - supabase/functions/instagram-oauth/index.test.ts
   - Tests should cover: OPTIONS 200, GET returns 405, POST without auth returns 401, POST with empty body returns 400, POST with malformed JSON returns 400
   - Use the test file pattern that conditionally runs tests based on env vars being set (see check-connection-health/index.test.ts for the pattern)
   - Tests can be run with: deno test --allow-net --allow-env

Leave untouched:
- Do NOT modify any other functions
- Do NOT modify _shared/* files
- Do NOT modify deno.json or config.toml
- Do NOT modify the action logic (authorize, callback, store_page, store_account, etc.) — only the request validation shell
- Do NOT modify package.json or any frontend code
- Do NOT add or remove any imports (but you may reorder usage)
- Do NOT git add or commit
</task>

<verification_loop>
Run these checks before finishing. Fix anything they surface:
  1. Read the modified files and verify the method/before-body-parsing restructuring is correct
  2. Read the modified files and verify every throw/catch path uses a proper HTTP status code (not all 500)
  3. Read the modified files and verify no env var values (FACEBOOK_APP_SECRET, INSTAGRAM_APP_SECRET, etc.) appear in any Response body
  4. Read the modified files and verify `await req.json()` is wrapped in `.catch(() => ({}))`
  5. Read the test files and verify they follow the pattern from check-connection-health/index.test.ts
  6. Run `git status --porcelain` to confirm only the two target index.ts files and the two test files are changed/created
</verification_loop>

<action_safety>
Scope strictly to the hardening described above. No unrelated refactors, no style changes, no renaming, no cleanup. Do NOT git add or git commit. Leave all changes in the working tree for review.
</action_safety>

<structured_output_contract>
Report:
1. Summary of changes made to each file
2. Files touched (full paths)
3. Verification outcomes (each check from <verification_loop> — pass/fail with evidence)
4. Any open questions or deviation from the brief
</structured_output_contract>
